class AppConstants {
  AppConstants._();

  // App Name
  static const String appName = 'Data Dash';

  // Shared Preferences or Secure Storage Keys
  static const String prefsUserToken = 'user_token';
  static const String prefsThemeMode = 'theme_mode';

  // Firebase Collections
  static const String collectionUsers = 'users';
  static const String collectionData = 'dash_data'; // for dashboard data

  // UI constants
  static const double defaultPadding = 16.0;
  static const double defaultBorderRadius = 12.0;
}
